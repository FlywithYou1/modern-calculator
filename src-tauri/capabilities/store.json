{
  "$schema": "../gen/schemas/capabilities.json",
  "identifier": "store",
  "description": "enable @tauri-apps/plugin-store default permissions for main window",
  "local": true,
  "windows": ["main"],
  "permissions": [
    "store:default"
  ]
}
